import 'package:flutter/material.dart';
class InviteExamScreen extends StatelessWidget {
  const InviteExamScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Invite and Exam'),
    );
  }
}
